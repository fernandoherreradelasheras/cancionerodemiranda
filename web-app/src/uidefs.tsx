export const Scale = {
    // Scale range in UI (1-5) mapped to Verovio scale range (10-100)
    MIN_SCALE: 1,
    MAX_SCALE: 5,
    MIN_VEROVIO_SCALE: 10,
    MAX_VEROVIO_SCALE: 100,
    DEFAULT_SCALE: 3
} as const